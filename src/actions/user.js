export const signup = (userInfo) => {
    return dispatch => {
        console.log(userInfo)
    }
}

export const login = (userInfo) => {
    return dispatch => {
        console.log(userInfo)
    }
}